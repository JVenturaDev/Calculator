name: Deploy Calculator

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Configure SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_LLAVE_PRIVADA }}

      - name: Set up Git identity
        run: |
          git config --global user.name "JVenturaDev"
          git config --global user.email "221253597+JVenturaDev@users.noreply.github.com"

      - name: Deploy to GitHub Pages
        run: npx gh-pages -d dist -r git@github.com:JVenturaDev/Calculator.git
